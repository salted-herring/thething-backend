/* ===========================
 * README
 * =========================== */
 
git hooks:
==============================
There is a pre-commit hook which creates a dump of the current database
into the config directory.

You'll need to update the USERNAME, PASSWORD & DATABASE in .git/hooks/pre-commit